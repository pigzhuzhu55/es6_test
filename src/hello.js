export default () => {
    var elements = [
        'Hydrogen',
        'Helium',
        'Lithium',
        'Beryllium'
      ];
      var s = elements.map(({ "length": lengthFooBArX }) => lengthFooBArX); 
     console.log(s);
};